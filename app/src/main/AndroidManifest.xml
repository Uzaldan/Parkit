<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".ParkingMap1"
            android:exported="false"
            android:label="@string/title_activity_parking_map1"
            android:theme="@style/Theme.MyApplication" />
        <activity
            android:name=".LotList"
            android:exported="false"
            android:label="@string/title_activity_lot_list"
            android:theme="@style/Theme.MyApplication" />

        <activity
            android:name=".Feedback"
            android:exported="false"
            android:label="@string/title_activity_feedback"
            android:theme="@style/Theme.MyApplication" />
        <activity
            android:name=".LotInfo"
            android:exported="false"
            android:label="@string/title_activity_lot_info"
            android:theme="@style/Theme.MyApplication" />
        <activity
            android:name=".EditLot"
            android:exported="false"
            android:label="@string/title_activity_edit_lot"
            android:theme="@style/Theme.MyApplication" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_API_KEY" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>